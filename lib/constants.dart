/// Global constants for the Lite Workspace application.
library;

/// Flag indicating if the application is currently running in Developer Mode.
/// When true, external API calls are skipped, returning mock data instantly.
const bool isDevMode = true;

/// Application theme and visual configuration constants.
class AppConstants {
  static const String appTitle = 'Lite Workspace';
}
